general {
    output_format = none
    colors = false
    interval = 1
}

order += "load"
order += "cpu_usage"
order += "time"

load {
    format = "load one: %1min"
}

cpu_usage {
    format = "cpu usage: %usage"
}

time {
    format = "%A, %B %d, %Y, %I:%M:%S %p"
}
