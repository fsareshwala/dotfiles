#!/usr/bin/env bash

directory="$HOME/.wallpapers"
if [[ -d "$directory" ]]; then
  cd "$directory"
  find . -maxdepth 1 -type f | shuf -n 1 | xargs -I {} feh --bg-fill {}
else
  xsetroot -solid black
fi
