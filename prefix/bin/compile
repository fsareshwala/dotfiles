#!/usr/bin/env bash

absolute_path=$(realpath "$1")
filename=$(basename "$absolute_path")
directory=$(dirname "$absolute_path")

case $absolute_path in
  *.tex)
    bazel build ...
    ;;
  *.c|*.cc)
    bazel build ...
    ;;
  *)
    echo "error: unsupported file ($absolute_path)"
    ;;
esac
